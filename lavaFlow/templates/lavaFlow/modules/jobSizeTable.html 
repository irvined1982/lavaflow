<table class="table table-striped">
	<thead>
		<tr>
			<th>Action</th>
			<th>Num Cores</th>
			<th># Runs</th>
			<th>Total CPU Time </th>
			<th>Total Wall Time </th>
			<th>Total Pend Time </th>
			<th>Pend as % Run</th> 
		</tr>
	</thead>
	<tbody>
		{% for r in rows %}
		<tr>
			<td>	
				<a href="#" rel="tooltip" data-toggle="tooltip" title="Only display jobs that used {{ r.num_processors }} processors."  onClick='addFilter("filter","num_processors","Num Processors","{{r.num_processors}}","{{r.num_processors}}");return false;'><l class="icon-filter"></a>
				<a href="#" rel="tooltip" data-toggle="tooltip" title="Do not display jobs that used {{r.num_processors }} processors." onClick='addFilter("exclude","num_processors","Num Processors","{{r.num_processors}}","{{r.num_processors}}");return false;'><l class="icon-minus"></l></a>
				<a href="#" rel="tooltip" data-toggle="tooltip" title="Remove all filters and excludes for jobs that used {{r.num_processors}} processors." onclick='removeAllFilters("num_processors","{{r.num_processors}}");return false;'><l class="icon-remove"></l></a>
			</td>
			<td>{{ r.num_processors }}</td>
			<td>{{ r.numRuns }}</td>
			<td title="Average: {{ r.avgCpuTime }}, Min: {{ r.minCpuTime }}, Max: {{ r.maxCpuTime }}" >{{ r.sumCpuTime }}</td>
			<td title="Average: {{ r.avgWallTime }}, Min: {{ r.minWallTime }}, Max: {{ r.maxWallTime }}" >{{ r.sumWallTime }}</td>
			<td title="Average: {{ r.avgPend_time }}, Min: {{ r.minPend_time }}, Max: {{ r.maxPend_time }}" >{{ r.sumPend_time }}</td>
			<td>{{ r.pendPct }}%</td>
		</tr>
		{%empty%}
		<tr><td colspan=7>No jobs for this time period.</td></tr>
		{% endfor %}
	</tbody>
</table>
<script type="text/javascript">
	$(function () {
		$("[rel='tooltip']").tooltip();
	});
</script>
