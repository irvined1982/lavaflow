{% extends "lavaFlow/base.html" %}
{% block main_body %}
		<h2>All Hosts</h2>
		<table class="tablesorter">
			<thead>
				<tr>
					<th>Host Name</th>
					<th>Submitted Jobs</th>
					<th>Executed Jobs</th>
				</tr>
			</thead>
			<tbody>
				{% for h in hosts.object_list %}
				<tr>
					<td><a href='{{h.get_absolute_url}}'>{{ h.hostName }}</a></td>
					<td>{{ h.submittedJobs }}</td>
					<td>{{ h.executedJobs }}</td>
				</tr>
				{% endfor %}
			</tbody>
		</table>
<div class="pagination">
	<span class="step-links">
		{% if hosts.has_previous %}
		<a href="?page={{hosts.previous_page_number}}">Previous</a>
		{% endif %}
		<span class="current">
			Page {{ hosts.number }} of {{ hosts.paginator.num_pages }}.
		</span>
		{% if hosts.has_next %}
		<a href="?page={{hosts.next_page_number}}">next</a>
		{% endif %}
	</span>
</div>
{% endblock %}
